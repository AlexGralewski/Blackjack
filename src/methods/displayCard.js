import React from "react"

function displayCard(cardCode) {
  //takes string argument where first letter is the value of the card and second is suit
  return(
    <div className="card"></div>
  )
}

export default displayCard